<div class="banner-grid">
    <?php $banners = $block->getBanners(); ?>
    <?php foreach ($banners as $banner) : ?>
        <div class="banner-item">
            <img src="<?php echo $banner['image']; ?>" alt="Banner">
            <a href="<?php echo $banner['url']; ?>" class="shop-now-button">SHOP NOW</a>
        </div>
    <?php endforeach; ?>
</div>
